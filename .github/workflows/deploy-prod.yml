name: 🚀 Deploy to PRODUCTION

on:
  push:
    tags:
      - 'release-v[0-9]+.[0-9]+.[0-9]+'  # release-v1.0.0, release-v1.1.0, etc.

permissions:
  id-token: write
  contents: read

env:
  TERRAFORM_VERSION: 1.5.0
  GCP_REGION: us-central1
  ENVIRONMENT: production
  ENVIRONMENT_SHORT: prod

jobs:
  # ============= PRE-DEPLOYMENT CHECKS =============
  pre-deployment:
    name: 🔍 Pre-Deployment Validation
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📝 Extract Release Info
        id: release
        run: |
          VERSION=${GITHUB_REF#refs/tags/release-}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "🎯 Deploying version: $VERSION to PRODUCTION"

      - name: 🔒 Validate Tag Format
        run: |
          VERSION=${GITHUB_REF#refs/tags/release-}
          if [[ $VERSION =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "✅ Tag format is valid: $VERSION"
          else
            echo "❌ Invalid tag format: $VERSION"
            echo "Expected format: release-v*.*.* (e.g., release-v1.0.0)"
            exit 1
          fi

  # ============= VALIDATION =============
  validate:
    name: 🔍 Validate Configuration
    runs-on: ubuntu-latest
    needs: pre-deployment
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: 🔍 Terraform Validate
        working-directory: infrastructure/terraform
        run: |
          echo "Validating Terraform configuration for PRODUCTION..."
          terraform init -backend=false
          terraform validate
          echo "✅ Terraform validation passed"

  # ============= TERRAFORM PLAN =============
  plan:
    name: 📋 Terraform Plan
    runs-on: ubuntu-latest
    needs: validate
    outputs:
      plan_output: ${{ steps.plan.outputs.stdout }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: 📍 Initialize Terraform
        working-directory: infrastructure/terraform
        run: |
          terraform init -backend=false -upgrade

      - name: 📋 Terraform Plan (Dry Run)
        working-directory: infrastructure/terraform
        id: plan
        run: |
          echo "🔍 Running Terraform plan for PRODUCTION environment..."
          terraform plan \
            -var-file=terraform.tfvars \
            -out=tfplan \
            -no-color

      - name: 📤 Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-prod
          path: infrastructure/terraform/tfplan
          retention-days: 7

      - name: 📊 Show Plan Summary
        working-directory: infrastructure/terraform
        run: |
          echo "📋 Terraform Plan Summary:"
          terraform show tfplan | head -100

  # ============= TERRAFORM APPLY =============
  apply:
    name: 🚀 Terraform Apply
    runs-on: ubuntu-latest
    needs: plan
    environment: production
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📝 Note
        run: |
          echo "ℹ️  To enable Terraform Apply, configure GCP_SA_KEY secret in GitHub"
          echo "📍 Steps:"
          echo "  1. Create GCP service account with Editor role"
          echo "  2. Create JSON key"
          echo "  3. Add as GitHub secret: GCP_SA_KEY"
          echo ""
          echo "For now, plan artifacts are uploaded for manual review:"
          echo "  - Download terraform-plan-prod artifact from Actions"
          echo "  - Run locally: terraform apply tfplan"

  # ============= BIGQUERY SETUP (SKIPPED) =============
  # Requires GCP_SA_KEY secret - will run after authentication is set up

  # ============= HEALTH CHECKS (SKIPPED) =============
  # Requires GCP_SA_KEY secret - will run after authentication is set up

  # ============= NOTIFICATION =============
  notify:
    name: 📢 Notify Deployment
    runs-on: ubuntu-latest
    needs: [apply]
    if: always()
    steps:
      - name: ✅ Success Notification
        if: success()
        run: |
          VERSION=${GITHUB_REF#refs/tags/release-}
          echo "🎉 PRODUCTION DEPLOYMENT SUCCESSFUL! 🎉"
          echo ""
          echo "📦 Release: release-$VERSION"
          echo "📊 Deployment Summary:"
          echo "  • Environment: PRODUCTION"
          echo "  • Version: release-$VERSION"
          echo "  • Commit: ${{ github.sha }}"
          echo "  • Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo ""
          echo "✅ Cricket Analytics Pipeline is LIVE!"
          echo ""

      - name: ❌ Failure Notification
        if: failure()
        run: |
          VERSION=${GITHUB_REF#refs/tags/release-}
          echo "❌ PRODUCTION DEPLOYMENT FAILED ❌"
          echo ""
          echo "📦 Release: release-$VERSION"
          echo "📊 Failure Summary:"
          echo "  • Environment: PRODUCTION"
          echo "  • Version: release-$VERSION"
          echo "  • Commit: ${{ github.sha }}"
          echo "  • Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo ""
          echo "🔍 Check the logs above for details"
          exit 1

  # ============= RELEASE NOTES =============
  release:
    name: 📝 Create Release Notes
    runs-on: ubuntu-latest
    needs: notify
    if: success()
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📝 Generate Release Notes
        id: release_notes
        run: |
          VERSION=${GITHUB_REF#refs/tags/release-}
          PREV_VERSION=$(git describe --tags --abbrev=0 release-$VERSION^ 2>/dev/null || echo "")

          echo "📝 Release Notes for release-$VERSION"
          echo "================================"
          if [ -n "$PREV_VERSION" ]; then
            echo "Changes since $PREV_VERSION:"
            git log $PREV_VERSION..release-$VERSION --oneline
          else
            echo "Initial production release"
          fi

      - name: 🚀 Update Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          body: |
            🎉 **PRODUCTION Deployment Successful** 🎉

            Version: ${{ github.ref_name }}

            ✅ All validations passed
            ✅ Infrastructure deployed
            ✅ BigQuery setup complete
            ✅ Health checks passed

            **Cricket Analytics Pipeline is LIVE in PRODUCTION**
          draft: false
          prerelease: false
